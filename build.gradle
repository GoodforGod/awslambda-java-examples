group = groupId
version = artifactVersion

subprojects {
    group = groupId
    version = artifactVersion

    configurations {
        all {
            resolutionStrategy {
                cacheChangingModulesFor 0, "seconds" // check for updates every build
            }
        }
    }
}